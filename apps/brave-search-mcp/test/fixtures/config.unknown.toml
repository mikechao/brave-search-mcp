[auth]
httpApiKey = "sk-test-config"

[auth.oauth]
issuer = "https://idp.example.com"
clientSecret = "oauth-secret"
badSecretRef = "wrong"

[audit]
enabled = true
hmacSecret = "audit-secret"

[guardrails]
requestLimit = 7

[server]
allowedHosts = [ "localhost" ]
